#ifndef _ERRORS_H
#define _ERRORS_H 1
enum HTERRORS {
  ERRHTINIT = 100,
  ERRHTGET,
  ERRHTINS,
  ERRHTDEL,
  ERRHTNOKEYLEN,
  ERRHTNOTABLE,
  ERRHTNOBUF,
  ERRHTNOITEM,
};
enum TOKENERRORS {
  ERRNOBYTES = 200,
  ERRNOBUFFER,
  ERRITERSTATE,
  ERRNOEXPR,
  ERRALLOC,
  ERRBUFFERINUSE,
  ERRNOTOKEN,
  ERRNOTOKENCOUNT,
};

int err_exit(char *, unsigned);
#endif
